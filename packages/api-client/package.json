{
  "name": "@nexzo/api-client",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cross-fetch": "^4.0.0"
  },
  "devDependencies": {
    "ts-jest": "^29.2.5"
  }
}
